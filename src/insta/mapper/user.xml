<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="User">
	<select id="getUserInfo" parameterType="string" resultType="userdto">
		SELECT * FROM USER_INFO WHERE USER_ID = #{userid}
	</select>
	<select id="getRecommendUser" parameterType="string" resultType="userdto">
		SELECT * FROM USER_INFO WHERE USER_ID NOT IN (SELECT FOLLOW_ID FROM FOLLOW WHERE USER_ID = #{userid}) AND USER_ID != #{userid}
	</select>
	
	<select id="getSearchInfo" parameterType="string" resultType="userdto">
		SELECT * FROM USER_INFO WHERE USER_ID LIKE ('%${search}%')
	</select>
	
	<select id="searchUserCnt" parameterType="string" resultType="_int">
		SELECT COUNT(*) FROM USER_INFO WHERE USER_ID LIKE ('%${search}%')
	</select>
	
	<select id="checkMyFollow" parameterType="hashmap" resultType="_int">
		SELECT COUNT(*) FROM FOLLOW WHERE USER_ID = #{loginUserId} AND FOLLOW_ID = #{userId}
	</select>
</mapper>